# Only what the recording needs. Everything else stays at Gatling's defaults so the
# corpus reflects a stock installation: utf-8 encoding, platform line separator.
gatling {
  data {
    writers = [console, file]
  }
}
